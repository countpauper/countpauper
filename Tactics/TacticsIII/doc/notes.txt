Line intersection for attacks.

Straight line first.
Parabola is a straight line (perpendiclar to gravity) a vector to span th eplane it is in (0,0,1) and a 3 coefficients along the line vector (every vector is a one)
The origin of the line is at the apex of the parabola .

Anyway line:
Line is an origin and vector.
For a line segment the vector spans the length of the line and the segment is [0...1]
For a half line the vector is unit (or speed or whatever) and the segment is [0...inf]
for a line the range is [-inf to inf ]

Each grid is an axis aligned box. The line starts at the character's shoot height in its grid on one of the 1-3 planes facing the target. (depends on the vector to the target. Any non 0 is a direction
The length of the line is the total fall off range (basically range + damage).

A list is computed with grid coordinates and the range of the line that goes through it.
This is done with a configurable epsilon so diagonal lines don't clip every tiny corner. They are a bit rounded for hitting terrain.
For every transition then the height is computed.
Then for each grid the height range in and out is computed and matched to the materials at those levels.
Each grid will get a getter to get a slice of materials (in order), with material, density, perhaps temperature.

Along the line the remaining damage of the attack is computed depending on the density and whether fire or poison is picked up.
Also obstacles/creatures are checked and the damage applied.

To compute the grids and the range that the line goes through, the scan starts at the origin of the line and the exiting face.
The neighbour at that face is selected.
The intersection with the line and all (other) faces of that line is computed.
Each face is an axis aligned plane
This is a new type of geometry that consists of:
- one float offset
- one axis aligned direction vector (X,Y,Z, or negative), stored as a Direction
Computing intersection with a line and this plane is relatively easy and fast math. It's just the delta along the axis between origin and offset divided by the vector's coefficient along that axis (if 0 then inf/-inf)
The nearest plane is found that is at least the epsilon away
The neighbour grid at that plane is taken next.
If the distance between the previous and the next plane is smaller than that epsilon, a corner is clipped and that
grid is skipped. Instead the epsilon is added to the next grid (or divided between previous and next?)

So: TODO:
[done] Make an axis aligned plane
[done] with functions to convert to a basic plane
[done] derived from IGeometry
[done] Add unit tests
[done] Implement the function to compute the line intersection.
[done] Range<double> Intersect(Line) const; which can return empty ranges and or infinite start end
[done] Add getters to the axis aligned bounding box to get Plane(Direction)
[done] Use these to implement the line intersection of the axis aligned box by: iterating over all of those planes.
[done] Check if the intersection coordinate is also within both perpendicular plane ranges.
[was alreadt done: Section] Create a function that creates a subsection line with that range, to get the height range over the Z
[done] unit test it of course
[done] Create a function to get a slice of materials from a grid, something
[done Apply the material section to the projectile
Apply the projectile to the obstacles for that grid
Orientation and origin surface adjustment (remove -1 on range)
Slow down by liquids 
Penetrate solids (use density + distance limits. Always limit by density that is impenetrable)
Penetrate objects 
Transfer target size to miss chance
Compute obscurement from vegation and add it to miss chance 
Add clouds to layers and compute cloud obscurement (might need redesign, how size of cloud is limited to layer? Split ? )
Projectile::Resistance(Materia)
[done] Then the final function to trace the path over multiple grids.
For melee weapons the same is done except the path is shorter and it may start at a different corner
[done] Reach: reach^2 = horizontal^2 + vertical^2 so vertical reach is sqrt ( reach^2 - horizontal^2) + - from attack height. The horizontal is computed using the normal manhattan distance like 

TODO:
[done] Where does it aim, how does (especially) vertical cover make it harder to aim.
[done] Instead of tracing a straight line and assuming perfect aim, a target height should be traced from the target to the origin
[done]This range is higher or smaller depending on the size of the character 
and what is aimed for (eg hamstring, headshot)
[done] Tracing through grids a slice can still be returned, but the height range is reduced by solid material (perhaps to 0)
[done] Then the algorithm will be: Trace two lines (bottom and top range) from aim target to source and keep the range of "clear" material
The hit chance is computed from the height of this range. THis could still be 0 so penetrating shots can be attempted
This also takes obstacles and their height into account
Then trace once from the source to the middle of this range (fairest) and see what it really will hit.



Having to take slces for (1) hit checks, is slow. Looks like slices are also needed for: 
2) Pathplanning 
3) Perception checks 
Using blocks is good for cellular automata, but ... the cell state is too big (>256bits) for precomputes cellular automata anyway 
Instead switch to a 2d grid of slices with layers. Layers are stores as a vector with thickness. 
Can perhaps still do cellular automata if each cell becomes about 1/256th meter (of course stored compressed with basically run length encoding)
Each cell is: 
material enum: about 4-5 bits 
temperature (state enum solid, liquid, gas): 3 bits
movement vector compressed into how many liters would flow up/or down, left or right, forward or backward per time unit. so that's 3still 3x5 bits or so and what does it mean if the unit of volume is 1 volumes

[done] Fix last attack tests
[done] Implement fixed point 
[done] Change Game::Position with const fixed point bits, (does it have tests) and make length/distance etc still work. fraction float can go, it's just intepretation. 
[done] Slice with fixed point and Fixed point aim height and Move with fixed point height  
[done] Update path planning to not use any blocks anymore, but use slices and vertical holes 
[done] Change the map to not store blocks but slices 
[done] Remove block altogether and rename BlockMap file, NB some slice tests still use blocks
[done] Make layer amount and temperature a fixed point/word
Slices for generating vertical mesh quads:
    - SLice::find(height): iterator to loop what's next to the surface. 
    - During Map::GenerateMesh() scan +X and +Y (-X and -Y are done by those opposites) for what is besides. 
    - If surface is stone and next is stone or veg and veg, or water+water make a side quad of the same to the height of that layer (add a function to compute the height up to it) 
    - If the surface is stone and next is veg then make a side of stone or the other way around 
    - if water and next is air, go one down to not air (stone, veg, water all will do) and make water side 
    - if water and next is stone/veg, then use stone veg 
    - if stone/veg and next is translucent (air or water) then go down to the first opaque (stone/veg), skip water and air, use that material for the side 
    - for the ones that go down (next to is air or water ) 



