`!dhclass [<class> <sub>] [<mastery level>] [<multi>]` Partial match of course

Look at the current character (!import does not import class) and export the missing class data, creating a Daggerheart character sheet. 
This should set the the appropriate: 
- Stress counter 
-  Hope counter (create with 2)
- Set empty experiences in cvar sheet Use !dhexperience to fill them in 
- Armor counter : how does this alias know the armor type? This is not `!import`ed

It chould also check and warn about mistakes at character creation (none imported).  
- AC is evasion, should match character class?  
- HP at character creation
- Check casting stat matching subclass 

The sheet will contain a json: 
class: <name>, subclass: <name>: experiences: { "name":"dnd name" }

If class and sub are specified a new sheet is made. 
Otherwise if a sheet exists the mastery level/multi class are modifed 
If nothing specified and no sheet exists it just gives a short help and the name of the class. 

`!dhexp [-]"<exp>" [...]` 
name experiences in order. It shows what scores were imported from the sheet and are now assigned. 
If [-] then the experience is deleted. TODO decide if the others shift over or a gap is left to keep the previous score 


`!dhlevel` Chose what bonuses to update in sheet (including +1 thresholds)
Also updates/checks the current sheet level (only updates if it's changed) ? An 
For stats updated in sheet, verify (eg proficiecnt)
For stats updates in var, change them

`!dh_short [armor [<ally>]] [stress] [heal [<ally>]] [hope[s]]`
`!dh_long  [armor [<ally>]] [stress] [heal [<ally>]] [hope[s]] [project <name>]`

armor: Repair 1d4+proficiency armor slots on short rest and all on long 
stress: Clear 1d4+proficiency stress on short rest and all on long 
health: Tend to 1d4+proficiency wounds on a short rest or all on long.
heal: Tend to 1d4+proficiency of an allies' wounds on a short rest or all on a long rest. 
TODO: this will give the `!g hp +<n>`  
hope: gain 1 hope by preparing.
hopes: gain 2 hopes by preparing with a friend.
project: Progress a project by 1. If it has a counter with the name it is increased


[mostly done]`!duality [<dice>=1h12+1f12] [<trait>] ["<experience>"]... [<dice>]... [adv/+] [dis/-]... [=<target>]

[done] Roll duality dice, by default roll 12 sides hope and fear. Override with `1h20` or `2f12` where `h` indicates hope dice and `f` fear dice. Then add bonuses `1h20+1d4`
[done] Trait (or 3 letter synonym) bonus is added. 
[done] experiences are checked in the sheet and their bonus added 
[done] one or more advantages or disadvantages are counted and added
[done] Add multiple fear and hope dice together 
[done] Hope is automatically increased and listed what the new counter is. In critical a stress is also automatically removed and shown.
[done, its both] experience should not be a number but a reference to a skill name 
[done] Target
* Now arguments separates by spaces are summed. The whole argument string could also be one big summation (subtraction)
 This basically preparses the library for a "duality dice" string. This string is split in hope, fear and generic bonuses. 
* +1f4 should add 1d4 to the basic 1d12 fear, not add it, but what is this syntax. Currently + are by default trunced off 
* If Seaborne/know the tide CC increase it on fear roll automatically. Then allow `tide` times to be added to add a +1 for each while reducing CC 


[done] `!dh_gm [<dice>] [<adv>] [<dis>]` Roll dm dice 
[done] Implementation: use 1d20 for the fear and hope None, that way there is no hope/fear because there's no comparison. 20 still crits.

[done] `!react <trait>[+"exp"][+<dice>]...[+adv]...[+dis]... [<difficulty>] [=<target>]` Same but instead of hope and fear it's just 2d12 
[done but use = syntax] any 2nd argument is the difficulty it will say success or fail. Crit is still success
[done] target is difficulty, can use string as well =easy, will say if made it 
[done] reactions can still critical, meaning all goals are made. 15% chance to make an impossible reaction, really? 

`!dhatk <weapon> [+<experience>] [+<dice>] [a[dv]]... [d[is]]... [=<avoidance>] [-th <major> <severe>] ` 

Attack with duality dice and the same bonuses. The weapon is taken from the sheet and its damage dice and types are used.
The damage dice are multiplied with the proficiency. If threshold is given it says how many hitpoints 
TODO it might end up looking funny but the alias needs to know the trait to use and attack bonus is not used. 
The smart sheet could select from attacks. Otherwise the player has to fill the right trait bonus in 
* Target


TODO: in initiative perhaps hitpoints can be applied on target by checking their avoidance, but need to find a way to 
get Daggerheart monster stats in bestiary, especially the thresholds (ac is avoidance and hp is hp holds).
Maybe through STR and DEX saves ... it's lame, but being able to use avrea initiative is not.
This doesn't work for player sheets however. They can only select save proficiency. 
Perhaps somehow reserve survival, stealth (and sleight of hand) skill for that and mess up the sheet to take that from the selected armor ? 
NB: 1 is the fixed implicit damage threshold for 1 wound 

`!dhcast` is like duality except the spell cast trait is automatically selected by checking the subclass 
Subsclass information goes in a gvar (with extensisiblity) and it may have cards in the same form as `!dhcard` that can be used 
The subclass mastery and specialization need to be flagged in the `!cvar dh_sheet`

`!dhcard <card>` find the partial card name in a gvar (SRD could be made available)
If there are any resources like hope or stress or armor, use them (if the counter is sufficient)
If that includes a duality dice then roll that. instead of trait it could be SPELL to pick the spell cast trait automatically
Then (based on the result/difficukty) it might describe what happens. No further automation for now. Check if the cards is known ? 

card dictionary: domain:s, level:n, [hope:n], [stress:n], [armor:n], [roll:str], 



Spells/cards are in the spellbook. with a gvar they can at least be looked up 

`!dhdeath` Make death move 
TBD 

`!dhstress [<amount>=1]` Mark a stress, become vulnerable if maxed out and reduce hp if over 

`!dhear [+/-[<n>]]` add or remove fear or just show it. Use a (configurable in svar/uvar?) evil smiley  

TODO: 
configuration svar:
Extreme damage threshold. A formula somehow based on other thresholds? Or get from skills ? 
Stack advantage and disadvantage keep highest one 
Change stress and hope modifier from crits? 








