<drac2>
args=&ARGS&
if not args:
    return f'help {ctx.alias} -here'

cards = dh.load_cards()
C=character()

card_name = args[0]

# Display like dh_lookup
# execute 
# 1 prevent if not available (with override 'allow')
# 2 deduct cost (with override 'free')
# 3 roll (with bonuses)
# Display results (counters, rolls) partially in fields 
</drac2>